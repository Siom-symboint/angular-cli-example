/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StructurallDirectiveDirective } from './structurall-directive.directive';

describe('Directive: StructurallDirective', () => {
  it('should create an instance', () => {
    const directive = new StructurallDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
